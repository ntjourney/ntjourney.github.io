import * as THREE from 'https://unpkg.com/three@0.162.0/build/three.module.js';
const host=document.getElementById('logo3d');
if(host){const reducedMotion=matchMedia('(prefers-reduced-motion: reduce)').matches,scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(34,1,.1,100);camera.position.set(0,.2,10);const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true,powerPreference:'low-power'});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.outputColorSpace=THREE.SRGBColorSpace;host.appendChild(renderer.domElement);const logo=new THREE.Group();scene.add(logo),scene.add(new THREE.AmbientLight(0xffffff,1.7));const key=new THREE.DirectionalLight(0xffffff,3.4);key.position.set(3,5,7),scene.add(key);const cyanLight=new THREE.DirectionalLight(0x66dce8,1.1);cyanLight.position.set(-5,1,4),scene.add(cyanLight);const black=new THREE.MeshPhysicalMaterial({color:0x080808,metalness:.78,roughness:.2,clearcoat:.55,clearcoatRoughness:.18}),silver=new THREE.MeshStandardMaterial({color:0xeceff0,metalness:.82,roughness:.25}),cyan=new THREE.MeshStandardMaterial({color:0x66dce8,metalness:.48,roughness:.27,emissive:0x0a2528,emissiveIntensity:.2});function poly(p){const s=new THREE.Shape();s.moveTo(...p[0]),p.slice(1).forEach(x=>s.lineTo(...x)),s.closePath();return s}function add(shape,depth,pos=[0,0,0]){const g=new THREE.ExtrudeGeometry(shape,{depth,bevelEnabled:true,bevelSegments:3,bevelSize:.045,bevelThickness:.045,curveSegments:3});g.center();const rim=new THREE.Mesh(g,silver);rim.scale.set(1.055,1.055,1.18),rim.position.set(pos[0],pos[1],-.07),logo.add(rim);const mesh=new THREE.Mesh(g,black);mesh.position.set(pos[0],pos[1],.08),logo.add(mesh)}add(poly([[-1.25,-1.8],[-.68,-1.8],[-.68,.2],[.6,-1.8],[1.24,-1.8],[1.24,1.8],[.67,1.8],[.67,-.22],[-.63,1.8],[-1.25,1.8]]),.34);add(poly([[-1.1,.8],[-2.2,1.62],[-2.48,1.43],[-1.48,.53],[-2.3,.85],[-2.48,.56],[-1.4,.18],[-2.05,.2],[-2.16,-.1],[-1.08,-.34]]),.28,[-.62,.45,0]);const accent=new THREE.Mesh(new THREE.BoxGeometry(.17,2.2,.12),cyan);accent.position.set(.92,0,-.29),accent.rotation.z=-.48,logo.add(accent);function resize(){const r=host.getBoundingClientRect();renderer.setSize(r.width,r.height,false),camera.aspect=r.width/r.height,camera.updateProjectionMatrix()}new ResizeObserver(resize).observe(host),resize();let prev=performance.now();function render(now){const dt=Math.min((now-prev)/1000,.05);prev=now,!reducedMotion&&(logo.rotation.y+=dt*Math.PI*2/10,logo.rotation.x=Math.sin(now*.00055)*.045),renderer.render(scene,camera),requestAnimationFrame(render)}requestAnimationFrame(render)}